{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "docker",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "App name",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    }
  },
  "required": ["name"]
}
